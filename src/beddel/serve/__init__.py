"""Beddel serve tier — delivery layer for runtime adapters.

This tier sits outside the domain core and depends inward only (serve → domain).
Public API exports will be wired in a subsequent task.
"""
