# Compatibility notice — this file is no longer a dependency source.
#
# Beddel development dependencies live in pyproject.toml's `dev` dependency
# group and their complete resolution is committed in uv.lock. From this
# directory, provision the pinned environment with:
#
#   uv sync --locked
#
# Keeping a second pip requirements list here would allow it to drift from the
# locked environment. Kit-specific dependencies remain outside the core SDK.
