# 20. Next Steps

1. **Story Drafting:** Draft Epic 1 stories with detailed task breakdowns using `@sm *create-next-story`, starting with Story 1.1 (Project Scaffolding, CI/CD & Documentation).
2. **Implementation:** Kiro (lead dev) implements stories following this architecture document. Domain core first, then primitives, then adapters, then integrations.
3. **Architect Review:** Submit architecture for validation by the architect agent (GPT-5.2) via the mailbox protocol — 1 session for architecture review before implementation begins.
4. **Spec Fixtures:** Create `spec/schemas/workflow.schema.json` and initial `spec/fixtures/` before implementing the parser (spec-first approach).
