# 11. Database Schema

N/A — Beddel is a stateless SDK with no database. State persistence (Epic 5) will use a pluggable storage backend (JSON initially, msgpack/DB later) but is out of scope for MVP.

---
