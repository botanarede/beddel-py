# 10. REST API Spec

N/A — Beddel is a Python SDK, not a REST API service. The FastAPI integration is an optional extra that exposes user-defined workflows as HTTP endpoints, but the SDK itself does not define a fixed REST API surface. The handler factory (`create_beddel_handler`) generates routes dynamically based on the workflow definition.

---
