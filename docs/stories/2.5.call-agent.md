# Story 2.5: Call-Agent Primitive (Nested Workflow Invocation)

## Status

Done

## Story

**As a** Python developer using Beddel,
**I want** a `call-agent` primitive that invokes another workflow by its agent ID,
**so that** I can compose complex behaviors by delegating to sub-workflows and reusing workflow definitions as building blocks.

## Acceptance Criteria

1. Register a `call-agent` async primitive function that accepts `(config: dict, context: ExecutionContext)` and returns the nested workflow's output
2. Extract `config["agentId"]` to identify the target workflow — raise `PrimitiveError` with `BEDDEL-EXEC-001` when missing
3. Extract `config["input"]` as the input data for the nested workflow (default to empty dict `{}` if not provided)
4. Retrieve a workflow loader callable from `context.metadata["workflow_loader"]` — raise `PrimitiveError` with `BEDDEL-EXEC-001` if not found
5. Call the workflow loader with the `agentId` to obtain a `WorkflowDefinition` — propagate `ParseError` if the loader fails
6. Create a new `WorkflowExecutor` using the registry from `context.metadata["registry"]` and execute the nested workflow with the resolved input
7. Return `ExecutionResult.output` from the nested workflow execution on success; raise `PrimitiveError` wrapping the error on failure
8. Support an optional `config["max_depth"]` (default: 5) to prevent infinite recursion — track depth via `context.metadata["call_depth"]` and raise `PrimitiveError` with `BEDDEL-EXEC-001` when exceeded
9. Log nested workflow invocation at DEBUG level via structured logging (agentId, depth, success/failure)
10. Unit tests achieve >80% coverage for `primitives/call_agent.py`

## Tasks / Subtasks

- [x] Task 1: Create `src/beddel-py/src/beddel/primitives/call_agent.py` with the `call_agent_primitive` function (AC: 1–9)
  - [x] 1.1 Define `async def call_agent_primitive(config: dict[str, Any], context: ExecutionContext) -> Any` (AC: 1)
  - [x] 1.2 Extract `config["agentId"]` — raise `PrimitiveError(BEDDEL-EXEC-001)` if missing (AC: 2)
  - [x] 1.3 Extract `config["input"]` with default `{}` (AC: 3)
  - [x] 1.4 Extract `config["max_depth"]` with default `5`; read current depth from `context.metadata["call_depth"]` (default `0`); raise `PrimitiveError(BEDDEL-EXEC-001)` if depth >= max_depth (AC: 8)
  - [x] 1.5 Retrieve `workflow_loader` from `context.metadata["workflow_loader"]` — raise `PrimitiveError(BEDDEL-EXEC-001)` if missing (AC: 4)
  - [x] 1.6 Call `workflow_loader(agentId)` to get `WorkflowDefinition` — propagate `ParseError` (AC: 5)
  - [x] 1.7 Retrieve `registry` from `context.metadata["registry"]`; create `WorkflowExecutor(registry)` and execute nested workflow with incremented `call_depth` in child context metadata (AC: 6, 8)
  - [x] 1.8 Return `ExecutionResult.output` on success; raise `PrimitiveError` wrapping error on failure (AC: 7)
  - [x] 1.9 Add DEBUG-level structured logging for invocation start, success, and failure (AC: 9)

- [x] Task 2: Register the call-agent primitive in `primitives/__init__.py` (AC: 1)
  - [x] 2.1 Import `call_agent_primitive` from `beddel.primitives.call_agent`
  - [x] 2.2 Add `registry.register_func("call-agent", call_agent_primitive)` in `register_builtins()`

- [x] Task 3: Write unit tests for the call-agent primitive (AC: 10)
  - [x] 3.1 Create `tests/unit/primitives/test_call_agent.py`
  - [x] 3.2 Test happy path: valid agentId + workflow_loader + registry → returns nested workflow output
  - [x] 3.3 Test missing `agentId` raises `PrimitiveError` with `BEDDEL-EXEC-001`
  - [x] 3.4 Test missing `workflow_loader` in metadata raises `PrimitiveError` with `BEDDEL-EXEC-001`
  - [x] 3.5 Test default input `{}` when `config["input"]` not provided
  - [x] 3.6 Test max_depth exceeded raises `PrimitiveError` with `BEDDEL-EXEC-001`
  - [x] 3.7 Test workflow_loader raising `ParseError` propagates as-is
  - [x] 3.8 Test nested workflow failure wraps error in `PrimitiveError`
  - [x] 3.9 Test custom `max_depth` from config is respected
  - [x] 3.10 Test `call_depth` is incremented in child context

- [x] Task 4: Run full validation suite (AC: 10)
  - [x] 4.1 Run `pytest tests/` — all tests pass
  - [x] 4.2 Run `ruff check .` — zero issues
  - [x] 4.3 Run `mypy src/` — zero errors

- [x] Task 5: Apply architect review corrections (A1–A5)
  - [x] 5.1 A1: Support async workflow loaders via `inspect.isawaitable()`
  - [x] 5.2 A2: Set `workflow_id=workflow_def.metadata.name` in child ExecutionContext
  - [x] 5.3 A3: Add clarifying comment on step_results dual-tracking pattern
  - [x] 5.4 A4: Null-safe error serialization + `workflow_id` in error details
  - [x] 5.5 A5: Add metadata passthrough test

## Dev Notes

### Architect Review

Reviewed by architect (GPT-5.2 via OpenClaw) on 2026-02-08. Verdict: PASS with CONCERNS.
Five corrections (A1–A5) applied. Full report: `.ai/report_call_agent.md`.
Future backlog items (B1–B5) tracked in the report for Phase 3+.

### Key Design Decisions

- `_execute_nested()` helper duplicates the executor step loop to propagate `call_depth` metadata. Architect approved for Phase 2; refactor to `execute_with_context()` planned for Phase 3 (B1).
- Async loader support via `inspect.isawaitable()` for backward compatibility with sync loaders.
- Child `ExecutionContext.workflow_id` set to `workflow_def.metadata.name` for traceability.
- Error serialization ensures `result.error` is never None in PrimitiveError details.

### File Locations

| File | Purpose | Action |
|------|---------|--------|
| `src/beddel-py/src/beddel/primitives/call_agent.py` | Call-agent primitive | Created |
| `src/beddel-py/src/beddel/primitives/__init__.py` | Registration helper | Modified |
| `src/beddel-py/tests/unit/primitives/test_call_agent.py` | Unit tests (12 tests) | Created |

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-08 | 1.0 | Story drafted from E2.5 epic requirements | Kiro (SM) |
| 2026-02-09 | 1.1 | Implementation complete, architect corrections A1–A5 applied | Kiro (DEV) |
| 2026-02-09 | 1.2 | SM review passed, status → Done, duplicate task block removed | Kiro (SM) |

## Dev Agent Record

### Agent Model Used
Kiro (Claude Opus 4.6) — implementation + SM review
architect (GPT-5.2 via OpenClaw) — design review

### Completion Notes
All 10 ACs met. 12 unit tests pass (10 original + 2 from architect corrections). Architect review PASS with 5 corrections applied (A1–A5). No domain core files modified. Full validation: 47/47 tests pass, ruff clean, mypy clean.

### File List
- `src/beddel-py/src/beddel/primitives/call_agent.py` (created)
- `src/beddel-py/src/beddel/primitives/__init__.py` (modified — added registration)
- `src/beddel-py/tests/unit/primitives/test_call_agent.py` (created)
