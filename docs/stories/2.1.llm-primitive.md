# Story 2.1: LLM Primitive (Single-Turn, Blocking)

## Status

Done

## Story

**As a** Python developer using Beddel,
**I want** an `llm` primitive that executes single-turn LLM calls through the ILLMProvider port,
**so that** I can define workflow steps that invoke language models with system prompts, user messages, and optional structured output schemas.

## Acceptance Criteria

1. Register an `llm` async primitive function that accepts `(config: dict, context: ExecutionContext)` and returns an `LLMResponse`
2. Build an `LLMRequest` from the resolved config: extract `model`, `messages`, `temperature`, `max_tokens`, `response_format` fields
3. Support a `system` shorthand in config that prepends a system message to the messages list
4. Support a `provider` field in config for future routing (stored but not used in this story — LiteLLM adapter handles routing)
5. Obtain the `ILLMProvider` instance from the `ExecutionContext.metadata["llm_provider"]` key
6. Call `provider.complete(request)` and return the `LLMResponse`
7. Raise `PrimitiveError` with `BEDDEL-EXEC-001` when `llm_provider` is missing from context metadata
8. Raise `ProviderError` with `BEDDEL-PROVIDER-001` when the provider call fails, wrapping the original exception
9. Unit tests achieve >80% coverage for `primitives/llm.py`

## Tasks / Subtasks

- [x] Task 1: Create `src/beddel-py/src/beddel/primitives/llm.py` with the `llm` primitive function (AC: 1, 2, 3, 4, 5, 6)
  - [x] 1.1 Define module-level `_build_request(config: dict) -> LLMRequest` helper that extracts model, messages, temperature, max_tokens, response_format from config dict
  - [x] 1.2 In `_build_request`, handle `system` shorthand: if config has `system` key, prepend `Message(role="system", content=system)` to the messages list
  - [x] 1.3 In `_build_request`, convert raw message dicts from config into `Message` model instances
  - [x] 1.4 Define `async def llm_primitive(config: dict[str, Any], context: ExecutionContext) -> LLMResponse` as the main entry point
  - [x] 1.5 Extract `ILLMProvider` from `context.metadata["llm_provider"]`, raise `PrimitiveError(BEDDEL-EXEC-001)` if missing (AC: 5, 7)
  - [x] 1.6 Call `await provider.complete(request)` and return the response (AC: 6)
  - [x] 1.7 Wrap provider exceptions in `ProviderError(BEDDEL-PROVIDER-001)` with original exception details (AC: 8)

- [x] Task 2: Create a registration helper and update `primitives/__init__.py` (AC: 1)
  - [x] 2.1 Add a `register_builtins(registry: PrimitiveRegistry) -> None` function in `primitives/__init__.py` that registers `llm_primitive` under the name `"llm"`
  - [x] 2.2 Import `llm_primitive` from `beddel.primitives.llm`

- [x] Task 3: Write unit tests for the `llm` primitive (AC: 9)
  - [x] 3.1 Create `tests/unit/primitives/test_llm.py` with proper `__init__.py` files
  - [x] 3.2 Test happy path: valid config with model + messages → LLMResponse returned
  - [x] 3.3 Test system shorthand: config with `system` key prepends system message
  - [x] 3.4 Test optional fields: temperature, max_tokens, response_format passed through to LLMRequest
  - [x] 3.5 Test default values: missing optional fields use LLMRequest defaults
  - [x] 3.6 Test error: missing `llm_provider` in context raises PrimitiveError with BEDDEL-EXEC-001
  - [x] 3.7 Test error: provider.complete() raises exception → wrapped in ProviderError with BEDDEL-PROVIDER-001
  - [x] 3.8 Test that `provider` field in config is ignored (not passed to LLMRequest)

- [x] Task 4: Run full validation suite (AC: 9)
  - [x] 4.1 Run `pytest tests/` — all tests pass
  - [x] 4.2 Run `ruff check .` — zero issues
  - [x] 4.3 Run `mypy src/` — zero errors

## Dev Notes

### Previous Story Insights

Stories 1.1–1.4 (Epic 1: Core Engine) are all Done. The domain core is fully implemented:
- `models.py`: All Pydantic models including `LLMRequest`, `LLMResponse`, `Message`, `TokenUsage`, `PrimitiveError`, `ProviderError`, `ErrorCode`
- `registry.py`: `PrimitiveRegistry` with `register()` decorator and `register_func()` imperative API
- `executor.py`: `WorkflowExecutor` calls `await primitive_fn(resolved_config, context)` — the primitive receives a resolved config dict and ExecutionContext
- `ports.py`: `ILLMProvider` protocol with `complete(request: LLMRequest) -> LLMResponse` and `stream(request: LLMRequest) -> AsyncIterator[str]`

Phase 0 scaffolding created all domain core files. No tests exist yet for primitives. The `primitives/` directory exists with an empty `__init__.py`.

### Data Models

The llm primitive works with these existing models from `beddel.domain.models`:

`LLMRequest`:
- `model: str` — Model identifier (e.g., "openrouter/anthropic/claude-3")
- `messages: list[Message]` — Conversation messages
- `temperature: float = 0.7` — Sampling temperature
- `max_tokens: int | None = None` — Maximum response tokens
- `response_format: dict[str, Any] | None = None` — Structured output schema
- `stream: bool = False` — Streaming flag (not used in this story)

`LLMResponse`:
- `content: str` — Response content
- `model: str` — Actual model used
- `usage: TokenUsage` — Token consumption (prompt_tokens, completion_tokens, total_tokens)
- `finish_reason: str = "stop"` — Completion reason
- `created_at: datetime` — Timestamp

`Message`:
- `role: str` — system | user | assistant
- `content: str`

`PrimitiveError(ExecutionError)` — for primitive-specific failures, uses `ErrorCode.EXEC_STEP_FAILED` (BEDDEL-EXEC-001)
`ProviderError(ExecutionError)` — for LLM provider failures, uses `ErrorCode.PROVIDER_ERROR` (BEDDEL-PROVIDER-001)

[Source: src/beddel-py/src/beddel/domain/models.py]

### ILLMProvider Port Interface

```python
@runtime_checkable
class ILLMProvider(Protocol):
    async def complete(self, request: LLMRequest) -> LLMResponse: ...
    async def stream(self, request: LLMRequest) -> AsyncIterator[str]: ...
```

The llm primitive only uses `complete()` in this story. Streaming is E2.2.

[Source: src/beddel-py/src/beddel/domain/ports.py]

### Primitive Function Signature

The executor calls primitives as: `await primitive_fn(resolved_config, context)`

Type: `PrimitiveFunc = Callable[[dict[str, Any], ExecutionContext], Awaitable[Any]]`

The config dict is already resolved by the VariableResolver before reaching the primitive. The primitive receives final values, not `$input.*` references.

[Source: src/beddel-py/src/beddel/domain/registry.py, executor.py]

### Provider Injection Pattern

The `ILLMProvider` instance is injected into `ExecutionContext.metadata["llm_provider"]` by the engine setup code (not yet implemented — will be in `create_engine()` factory). For this story, tests mock the provider and inject it into context metadata.

[Source: docs/architecture/components.md#llm Primitive]

### YAML Config Shape (from fixtures)

```yaml
- id: "greet"
  type: "llm"
  config:
    provider: "google"
    model: "gemini-2.0-flash-exp"
    system: "You are a helpful assistant."
    messages:
      - role: "user"
        content: "$input.prompt"
  result: "greeting"
```

After variable resolution, config arrives as:
```python
{
    "provider": "google",
    "model": "gemini-2.0-flash-exp",
    "system": "You are a helpful assistant.",
    "messages": [{"role": "user", "content": "Hello world"}],
}
```

[Source: src/beddel-py/tests/fixtures/workflows/simple.yaml]

### File Locations

| File | Purpose |
|------|---------|
| `src/beddel-py/src/beddel/primitives/llm.py` | LLM primitive implementation (create) |
| `src/beddel-py/src/beddel/primitives/__init__.py` | Registration helper (update) |
| `src/beddel-py/tests/unit/primitives/__init__.py` | Test package init (create) |
| `src/beddel-py/tests/unit/primitives/test_llm.py` | LLM primitive unit tests (create) |
| `src/beddel-py/src/beddel/domain/models.py` | LLMRequest, LLMResponse, Message, errors (read-only) |
| `src/beddel-py/src/beddel/domain/ports.py` | ILLMProvider protocol (read-only) |
| `src/beddel-py/src/beddel/domain/registry.py` | PrimitiveRegistry (read-only) |

### Technical Constraints

- Python 3.11+ with async/await [Source: docs/architecture/tech-stack.md]
- All I/O functions MUST be async [Source: docs/architecture/coding-standards.md]
- Type hints required on all public functions [Source: docs/architecture/coding-standards.md]
- `from __future__ import annotations` for deferred evaluation
- Structured logging via `logging.getLogger("beddel.primitives.llm")`
- No secrets in code — API keys come from environment variables via provider adapter [Source: docs/architecture/coding-standards.md]

### Testing

- Framework: pytest 8.3+ with pytest-asyncio 0.25+ (asyncio_mode = "auto")
- Location: `tests/unit/primitives/test_llm.py`
- Coverage: >80% for `primitives/llm.py`
- Pattern: AAA (Arrange, Act, Assert)
- Mock: `ILLMProvider` — create an async mock that returns a predefined `LLMResponse`
- Inject mock provider via `ExecutionContext(metadata={"llm_provider": mock_provider})`
- Key scenarios: happy path, system shorthand, optional fields, missing provider error, provider failure error
- Run commands from `src/beddel-py/`: `pytest`, `ruff check .`, `mypy src/`

[Source: docs/architecture/test-strategy-and-standards.md]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-08 | 1.0 | Story drafted from E2.1 epic requirements | Bob (SM) |
| 2026-02-08 | 1.1 | Tasks 1-4 implemented, all validations passing | Kiro (DEV) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (Kiro)

### Debug Log References
N/A — no issues encountered

### Completion Notes
- Task 1 (llm primitive) was already complete from prior session
- Task 2: Added `register_builtins()` in `primitives/__init__.py` with TC001-compliant imports
- Task 3: 9 unit tests covering all AC scenarios — happy path, system shorthand, optional fields, defaults, error cases, provider field ignored, plus `_build_request` helpers
- Task 4: Full validation suite green — pytest 9/9, ruff clean, mypy clean
- No domain core files modified (read-only as required)
- No new dependencies added

### File List
- `src/beddel-py/src/beddel/primitives/__init__.py` (modified — added `register_builtins`)
- `src/beddel-py/tests/unit/primitives/test_llm.py` (created — 9 unit tests)
