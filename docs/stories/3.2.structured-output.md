# Story 3.2: Structured Output Enforcement (Pydantic)

## Status

InProgress

## Story

**As a** Python developer using Beddel,
**I want** structured output enforcement that validates LLM responses against Pydantic models,
**so that** my workflows can reliably extract typed, validated data from LLM calls without manual JSON parsing or error handling.

## Acceptance Criteria

1. Create a `StructuredOutputHandler` class in `src/beddel-py/src/beddel/adapters/structured.py` that takes a Pydantic model class and produces the `response_format` dict for LiteLLM
2. Implement `def to_response_format(self) -> dict[str, Any]` that converts a Pydantic model into a JSON Schema `response_format` parameter compatible with LiteLLM's structured output API (`{"type": "json_schema", "json_schema": {"name": ..., "strict": True, "schema": ...}}`)
3. Implement `def parse_response(self, content: str) -> BaseModel` that parses raw LLM response content (JSON string) into a validated Pydantic model instance
4. Raise `PrimitiveError` with `BEDDEL-EXEC-001` when LLM response content is not valid JSON
5. Raise `PrimitiveError` with `BEDDEL-EXEC-001` when parsed JSON does not conform to the Pydantic model schema (validation error)
6. Integrate with the `llm` primitive: when `config["response_model"]` is set, automatically apply `StructuredOutputHandler` to build `response_format` and parse/validate the response
7. The `llm` primitive returns the validated Pydantic model instance (as a dict via `.model_dump()`) instead of raw `LLMResponse` when `response_model` is configured
8. Support nested Pydantic models, `Optional` fields, `list[...]` fields, and `Literal` types in the schema generation
9. Export `StructuredOutputHandler` from `beddel.adapters`
10. Unit tests achieve >80% coverage for `adapters/structured.py` and the new `llm` primitive structured output path

## Tasks / Subtasks

- [x] Task 1: Create `src/beddel-py/src/beddel/adapters/structured.py` with `StructuredOutputHandler` (AC: 1, 2, 3, 8)
  - [x] 1.1 Define `class StructuredOutputHandler` with `__init__(self, model: type[BaseModel])` storing the Pydantic model class (AC: 1)
  - [x] 1.2 Implement `to_response_format(self) -> dict[str, Any]` using `model.model_json_schema()` to generate JSON Schema, wrapped in LiteLLM's expected format (AC: 2, 8)
  - [x] 1.3 Implement `parse_response(self, content: str) -> BaseModel` that calls `json.loads()` then `model.model_validate()` (AC: 3)
  - [x] 1.4 Raise `PrimitiveError(BEDDEL-EXEC-001)` on `json.JSONDecodeError` with details including raw content snippet (AC: 4)
  - [x] 1.5 Raise `PrimitiveError(BEDDEL-EXEC-001)` on `pydantic.ValidationError` with details including validation errors (AC: 5)
  - [x] 1.6 Add structured DEBUG logging via `logging.getLogger("beddel.adapters.structured")` for schema generation and parse attempts

- [x] Task 2: Integrate structured output into `llm` primitive (AC: 6, 7)
  - [x] 2.1 In `_build_request()`: when `config["response_model"]` is present, instantiate `StructuredOutputHandler` and set `response_format` from `handler.to_response_format()` (AC: 6)
  - [x] 2.2 In `llm_primitive()`: when `response_model` was configured, call `handler.parse_response(response.content)` and return `model_instance.model_dump()` instead of raw `LLMResponse` (AC: 7)
  - [x] 2.3 Ensure streaming path raises `PrimitiveError` if `response_model` is set (structured output is blocking-only)

- [ ] Task 3: Update `adapters/__init__.py` with exports (AC: 9)
  - [ ] 3.1 Export `StructuredOutputHandler` from `beddel.adapters`

- [ ] Task 4: Write unit tests for `StructuredOutputHandler` (AC: 10)
  - [ ] 4.1 Create `tests/unit/adapters/test_structured.py`
  - [ ] 4.2 Test `to_response_format()` produces correct LiteLLM-compatible JSON Schema dict for a simple model
  - [ ] 4.3 Test `to_response_format()` handles nested models, `Optional`, `list`, and `Literal` fields (AC: 8)
  - [ ] 4.4 Test `parse_response()` happy path: valid JSON → validated Pydantic instance
  - [ ] 4.5 Test `parse_response()` with invalid JSON → `PrimitiveError(BEDDEL-EXEC-001)` (AC: 4)
  - [ ] 4.6 Test `parse_response()` with valid JSON but schema mismatch → `PrimitiveError(BEDDEL-EXEC-001)` (AC: 5)

- [ ] Task 5: Write unit tests for `llm` primitive structured output integration (AC: 10)
  - [ ] 5.1 Test `llm_primitive()` with `response_model` in config: verify `response_format` is passed to provider and response is parsed/validated
  - [ ] 5.2 Test `llm_primitive()` with `response_model` returns `.model_dump()` dict (AC: 7)
  - [ ] 5.3 Test `llm_primitive()` with `response_model` + `stream=True` raises `PrimitiveError`
  - [ ] 5.4 Test `llm_primitive()` without `response_model` still works as before (regression)

- [ ] Task 6: Run full validation suite (AC: 10)
  - [ ] 6.1 Run `pytest tests/` — all tests pass
  - [ ] 6.2 Run `ruff check .` — zero issues
  - [ ] 6.3 Run `mypy src/` — zero errors

## Dev Notes

### Previous Story Insights (from E3.1 — LiteLLM Adapter)

Story 3.1 established the LiteLLM adapter with the `ILLMProvider` protocol. Key patterns:
- `LiteLLMAdapter._build_params()` already passes `response_format` through to `litellm.acompletion()` when set on `LLMRequest`
- `LLMRequest.response_format` is typed as `dict[str, Any] | None` — this is the slot where structured output schema goes
- The `llm` primitive's `_build_request()` already forwards `config["response_format"]` to `LLMRequest`
- QA noted CODE-001 (litellm.Usage fallback) and ARCH-002 (unused `LLMRequest.stream` field) as low-severity items — not blockers for this story

[Source: docs/stories/3.1.litellm-adapter.md — QA Results]

### LiteLLM Structured Output Format

LiteLLM expects `response_format` in this shape for JSON Schema enforcement:

```python
{
    "type": "json_schema",
    "json_schema": {
        "name": "ModelName",       # from model.__name__
        "strict": True,
        "schema": { ... }          # from model.model_json_schema()
    }
}
```

This is passed directly to the provider (OpenAI, Gemini, etc.) which enforces the schema at generation time. The LLM response content will be a JSON string conforming to the schema.

[Source: docs/architecture/external-apis.md, https://docs.litellm.ai/]

### Pydantic JSON Schema Generation

Pydantic v2 provides `model.model_json_schema()` which generates a JSON Schema dict from a model class. This handles:
- Nested models (via `$defs` references)
- `Optional[T]` fields (anyOf with null)
- `list[T]` fields (array type with items)
- `Literal["a", "b"]` (enum constraint)
- Field descriptions, defaults, constraints

The generated schema can be passed directly as the `schema` value in the LiteLLM format.

[Source: Pydantic v2 documentation]

### How the LLM Primitive Currently Handles response_format

In `primitives/llm.py`, `_build_request()` already checks for `config["response_format"]` and passes it to `LLMRequest`. The new `response_model` config key is a higher-level abstraction: the user provides a Pydantic model class, and the handler converts it to the low-level `response_format` dict automatically.

Flow with `response_model`:
```
config["response_model"] = MyModel
  → StructuredOutputHandler(MyModel)
  → handler.to_response_format() → LLMRequest.response_format
  → LiteLLMAdapter passes to litellm.acompletion()
  → LLM returns JSON string
  → handler.parse_response(content) → MyModel instance
  → instance.model_dump() → returned as step result
```

Flow without `response_model` (unchanged):
```
config["response_format"] = {...}  (optional, raw dict)
  → LLMRequest.response_format = {...}
  → LiteLLMAdapter passes to litellm.acompletion()
  → LLMResponse returned as-is
```

[Source: src/beddel-py/src/beddel/primitives/llm.py]

### Data Models Used

`LLMRequest` (from `beddel.domain.models`):
- `response_format: dict[str, Any] | None = None` — the slot for structured output schema

`PrimitiveError(ExecutionError)` — for primitive-specific failures:
- `ErrorCode.EXEC_STEP_FAILED` (`BEDDEL-EXEC-001`)

[Source: src/beddel-py/src/beddel/domain/models.py]

### File Locations

| File | Purpose | Action |
|------|---------|--------|
| `src/beddel-py/src/beddel/adapters/structured.py` | StructuredOutputHandler | Create |
| `src/beddel-py/src/beddel/adapters/__init__.py` | Adapter exports | Modify (add StructuredOutputHandler) |
| `src/beddel-py/src/beddel/primitives/llm.py` | LLM primitive | Modify (add response_model integration) |
| `src/beddel-py/tests/unit/adapters/test_structured.py` | Handler unit tests | Create |
| `src/beddel-py/tests/unit/primitives/test_llm.py` | LLM primitive tests | Modify (add structured output tests) |
| `src/beddel-py/src/beddel/domain/models.py` | Domain models | Read-only |
| `src/beddel-py/src/beddel/domain/ports.py` | ILLMProvider Protocol | Read-only |

### Source Tree Context

```
src/beddel-py/src/beddel/
├── adapters/
│   ├── __init__.py     # Add StructuredOutputHandler export
│   ├── litellm.py      # Existing — passes response_format through (no changes)
│   └── structured.py   # NEW: StructuredOutputHandler class
├── domain/
│   ├── models.py        # LLMRequest.response_format, PrimitiveError
│   └── ports.py         # ILLMProvider Protocol (read-only)
└── primitives/
    └── llm.py           # Modify: response_model → StructuredOutputHandler integration
```

[Source: docs/architecture/source-tree.md]

### Technical Constraints

- Python 3.11+ with async/await [Source: docs/architecture/tech-stack.md]
- `from __future__ import annotations` for deferred evaluation [Source: docs/architecture/coding-standards.md]
- Type hints required on all public functions [Source: docs/architecture/coding-standards.md]
- Structured logging via named loggers with %-style formatting [Source: docs/architecture/coding-standards.md]
- Pydantic 2.10+ already in dependencies [Source: docs/architecture/tech-stack.md]
- No new dependencies required — uses `pydantic.BaseModel.model_json_schema()` and `json.loads()` from stdlib
- Ruff 0.9+ for linting, mypy 1.14+ strict [Source: docs/architecture/coding-standards.md]

### Testing

- **Framework**: pytest 8.3+ with pytest-asyncio 0.25+ (asyncio_mode = "auto")
- **Locations**: `tests/unit/adapters/test_structured.py` (new), `tests/unit/primitives/test_llm.py` (modify)
- **Coverage**: >80% for `adapters/structured.py` and the new llm primitive code paths
- **Pattern**: AAA (Arrange, Act, Assert)
- **Mocking**: Mock `ILLMProvider` via `AsyncMock` for llm primitive tests — no real API calls
- **Key scenarios**: schema generation (simple, nested, Optional, list, Literal), parse happy path, parse invalid JSON, parse schema mismatch, llm primitive integration with response_model, streaming rejection, regression without response_model
- **Run commands** from `src/beddel-py/`: `pytest`, `ruff check .`, `mypy src/`

[Source: docs/architecture/test-strategy-and-standards.md]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-09 | 1.0 | Story drafted from E3.2 epic requirements | Kiro (SM) |

## Dev Agent Record

### Agent Model Used
_To be filled during implementation_

### Debug Log References
_To be filled during implementation_

### Completion Notes
_To be filled during implementation_

### File List
_To be filled during implementation_

## QA Results
_To be filled during QA review_
