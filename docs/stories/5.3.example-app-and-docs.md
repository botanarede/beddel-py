# Story 5.3: Example Application and Documentation

## Status

Done

## Story

**As a** Python developer evaluating or onboarding to Beddel,
**I want** a working example FastAPI application with accompanying documentation that demonstrates the full Beddel workflow lifecycle (blocking and SSE streaming),
**so that** I can quickly understand how to integrate Beddel into my own web application, see the SDK's public API in action, and have a reference implementation to copy from.

## Acceptance Criteria

1. Create an example FastAPI application in `src/beddel-py/examples/fastapi_app.py` that exposes at least two endpoints: one blocking workflow and one SSE streaming workflow
2. Create example YAML workflow files in `src/beddel-py/examples/workflows/` — at minimum a `simple.yaml` (single-step blocking) and a `streaming.yaml` (single-step with `stream: true`)
3. The example app uses `create_beddel_handler` from `beddel.integrations.fastapi` to mount workflow handlers on FastAPI routes
4. The example app includes a health check endpoint (`GET /health`) that returns `{"status": "ok"}`
5. The example app is runnable with `uvicorn examples.fastapi_app:app` from the `src/beddel-py/` directory (after installing `beddel[fastapi]`)
6. Update `src/beddel-py/README.md` with a Quick Start section that covers: installation (`pip install beddel` and `pip install beddel[fastapi]`), a minimal code example, and instructions to run the example app
7. The README documents the public API surface: `create_beddel_handler`, `BeddelSSEAdapter`, `SSEEvent`, and key domain types (`WorkflowDefinition`, `ExecutionResult`, `WorkflowExecutor`)
8. Add inline comments in the example app explaining each section (imports, workflow loading, handler creation, route mounting)
9. Write a smoke test in `tests/integration/test_example_app.py` that imports the example app, sends a request to `/health`, and verifies the response — ensuring the example doesn't bit-rot
10. All existing tests continue to pass after changes

## Tasks / Subtasks

- [x] Task 1: Create example workflow YAML files (AC: 2)
  - [x] 1.1 Create `src/beddel-py/examples/` directory with an `__init__.py`
  - [x] 1.2 Create `src/beddel-py/examples/workflows/` directory
  - [x] 1.3 Create `examples/workflows/simple.yaml` — single-step `llm` workflow (blocking mode) with `$input.prompt` variable
  - [x] 1.4 Create `examples/workflows/streaming.yaml` — single-step `llm` workflow with `stream: true` and `$input.prompt` variable

- [x] Task 2: Create the example FastAPI application (AC: 1, 3, 4, 5, 8)
  - [x] 2.1 Create `src/beddel-py/examples/fastapi_app.py` with module docstring explaining purpose and how to run
  - [x] 2.2 Import `FastAPI`, `create_beddel_handler` from `beddel.integrations.fastapi`, and `YAMLParser` from `beddel`
  - [x] 2.3 Create `FastAPI(title="Beddel Example", version="0.1.0")` app instance
  - [x] 2.4 Add `GET /health` endpoint returning `{"status": "ok"}`
  - [x] 2.5 Load `simple.yaml` and `streaming.yaml` workflows via `YAMLParser().parse_file()`
  - [x] 2.6 Create handlers via `create_beddel_handler(workflow)` for each workflow
  - [x] 2.7 Mount handlers on `POST /agent/simple` and `POST /agent/streaming` routes
  - [x] 2.8 Add `if __name__ == "__main__"` block with `uvicorn.run(app, host="0.0.0.0", port=8000)`
  - [x] 2.9 Add inline comments explaining each section (imports, workflow loading, handler creation, route mounting, startup)

- [x] Task 3: Update README.md with Quick Start and API documentation (AC: 6, 7)
  - [x] 3.1 Add "Quick Start" section with installation commands (`pip install beddel`, `pip install beddel[fastapi]`)
  - [x] 3.2 Add minimal code example showing `create_beddel_handler` usage (5-10 lines)
  - [x] 3.3 Add "Running the Example" subsection with `uvicorn` command and `curl` examples for both blocking and streaming endpoints
  - [x] 3.4 Add "API Reference" section documenting: `create_beddel_handler`, `BeddelSSEAdapter`, `SSEEvent`, `WorkflowDefinition`, `ExecutionResult`, `WorkflowExecutor`
  - [x] 3.5 Add "Project Structure" section showing the SDK layout

- [x] Task 4: Write smoke test for the example app (AC: 9, 10)
  - [x] 4.1 Create `tests/integration/test_example_app.py`
  - [x] 4.2 Import the example app and create a `TestClient` (or `httpx.AsyncClient` with `ASGITransport`)
  - [x] 4.3 Test `GET /health` returns 200 with `{"status": "ok"}`
  - [x] 4.4 Test `POST /agent/simple` endpoint is reachable (mock executor to avoid real LLM calls)

- [x] Task 5: Run full validation suite (AC: 10)
  - [x] 5.1 Run `pytest tests/` — all tests pass (including new smoke test and all existing tests)
  - [x] 5.2 Run `ruff check .` — zero issues
  - [x] 5.3 Run `mypy src/` — zero errors

## Dev Notes

### Previous Story Insights (from E5.2 — SSE Streaming Response Adapter)

Story 5.2 extracted SSE logic into a framework-agnostic `BeddelSSEAdapter` in `integrations/sse.py`. Key patterns:
- `BeddelSSEAdapter` is stateless — safe for concurrent use, instantiate per-request or share
- `SSEEvent` dataclass with `serialize()` method for raw wire format
- `fastapi.py` was refactored to use `BeddelSSEAdapter` via `_sse_bridge()` — converts `SSEEvent` instances to dicts for `EventSourceResponse`
- QA tracked SSE-001 (private `_build_error_event` imported across module boundary) as medium severity — not blocking, tracked for cleanup
- `integrations/__init__.py` exports `BeddelSSEAdapter` and `SSEEvent` unconditionally, `create_beddel_handler` conditionally (requires FastAPI)

[Source: docs/stories/5.2.sse-streaming-adapter.md — Dev Notes, QA Results]

### Architecture Context

The example application demonstrates the integration layer consuming the domain core. The architecture flow is:

```
User → FastAPI → create_beddel_handler → WorkflowExecutor → Primitives → LiteLLMAdapter
```

For SSE streaming:
```
User → FastAPI → EventSourceResponse → BeddelSSEAdapter → WorkflowExecutor → AsyncIterator[str]
```

The example app is NOT part of the SDK package — it lives in `examples/` alongside the source, serving as documentation and a reference implementation.

[Source: docs/architecture/core-workflows.md, docs/architecture/components.md]

### Public API Surface (for README documentation)

```python
# Core domain types
from beddel import (
    WorkflowDefinition,
    WorkflowExecutor,
    ExecutionResult,
    YAMLParser,
    PrimitiveRegistry,
    VariableResolver,
)

# Integration layer
from beddel.integrations import BeddelSSEAdapter, SSEEvent
from beddel.integrations.fastapi import create_beddel_handler

# Error types
from beddel import (
    BeddelError,
    ParseError,
    ConfigurationError,
    ExecutionError,
    ProviderError,
)
```

[Source: src/beddel-py/src/beddel/__init__.py, src/beddel-py/src/beddel/integrations/__init__.py]

### `create_beddel_handler` Factory Signature

```python
def create_beddel_handler(
    workflow: WorkflowDefinition | str,
    *,
    provider: ILLMProvider | None = None,
    hooks: list[ILifecycleHook] | None = None,
    tracer: ITracer | None = None,
    registry: PrimitiveRegistry | None = None,
) -> Callable[..., Awaitable[Any]]:
```

Accepts a `WorkflowDefinition` or a `str` path to a YAML file. Returns an async handler that accepts a `Request` and returns `JSONResponse` (blocking) or `EventSourceResponse` (streaming).

[Source: src/beddel-py/src/beddel/integrations/fastapi.py]

### Existing Workflow Fixtures (reference for example workflows)

Test fixtures in `tests/fixtures/workflows/`:
- `simple.yaml` — single `llm` step with `$input.prompt`
- `multi_step.yaml` — 3-step pipeline (analyze → summarize → format)
- `nested_agent.yaml` — `call-agent` + `guardrail` steps

The example workflows should be similar but tailored for documentation clarity (more descriptive metadata, comments explaining each field).

[Source: src/beddel-py/tests/fixtures/workflows/]

### File Locations

| File | Purpose | Action |
|------|---------|--------|
| `src/beddel-py/examples/__init__.py` | Package marker | Create |
| `src/beddel-py/examples/fastapi_app.py` | Example FastAPI application | Create |
| `src/beddel-py/examples/workflows/simple.yaml` | Blocking workflow example | Create |
| `src/beddel-py/examples/workflows/streaming.yaml` | Streaming workflow example | Create |
| `src/beddel-py/README.md` | Package documentation | Modify |
| `src/beddel-py/tests/integration/test_example_app.py` | Smoke test | Create |

### Source Tree Context

```
src/beddel-py/
├── examples/                   # NEW: Example applications
│   ├── __init__.py
│   ├── fastapi_app.py          # NEW: Example FastAPI app
│   └── workflows/
│       ├── simple.yaml         # NEW: Blocking workflow
│       └── streaming.yaml      # NEW: Streaming workflow
├── src/
│   └── beddel/
│       ├── integrations/
│       │   ├── __init__.py     # Read-only (exports BeddelSSEAdapter, SSEEvent, create_beddel_handler)
│       │   ├── fastapi.py      # Read-only (create_beddel_handler factory)
│       │   └── sse.py          # Read-only (BeddelSSEAdapter, SSEEvent)
│       └── ...
├── tests/
│   ├── integration/
│   │   └── test_example_app.py # NEW: Smoke test for example app
│   └── ...
├── README.md                   # Modify: add Quick Start + API docs
└── pyproject.toml              # Read-only
```

[Source: docs/architecture/source-tree.md]

### Technical Constraints

- Python 3.11+ with async/await [Source: docs/architecture/tech-stack.md]
- `from __future__ import annotations` for deferred evaluation [Source: docs/architecture/coding-standards.md]
- Type hints required on all public functions [Source: docs/architecture/coding-standards.md]
- Example app requires `beddel[fastapi]` extra (FastAPI + uvicorn + sse-starlette) [Source: src/beddel-py/pyproject.toml]
- Example workflows use `$input.*` variable syntax for user input [Source: docs/architecture/components.md]
- The `examples/` directory is NOT included in the wheel package (only `src/beddel/` is packaged) [Source: src/beddel-py/pyproject.toml — hatch.build.targets.wheel]

### Testing

- **Framework**: pytest 8.3+ with pytest-asyncio 0.25+ (asyncio_mode = "auto")
- **Location**: `tests/integration/test_example_app.py` (new)
- **Pattern**: AAA (Arrange, Act, Assert)
- **HTTP Testing**: Use `httpx.AsyncClient` with `ASGITransport` for async endpoint testing
- **Mocking**: Mock `WorkflowExecutor.execute()` to avoid real LLM calls in smoke tests
- **Run commands** from `src/beddel-py/`: `pytest`, `ruff check .`, `mypy src/`

[Source: docs/architecture/test-strategy-and-standards.md]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-10 | 1.0 | Story drafted from E5.3 epic requirements | Kiro (SM) |

## Dev Agent Record

### Agent Model Used
_To be filled during implementation_

### Debug Log References
_To be filled during implementation_

### Completion Notes
_To be filled during implementation_

### File List
_To be filled during implementation_

## QA Results

### Review Date: 2026-02-10

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementation is clean and well-structured. The example FastAPI app follows hexagonal architecture correctly — it sits outside the SDK package boundary and consumes only the public API surface (`create_beddel_handler`, `YAMLParser`). Code uses `from __future__ import annotations` consistently, type hints on all public functions, and the AAA test pattern. Inline comments are thorough without being excessive — each section (imports, app instance, health check, workflow loading, handler creation, route mounting, startup) is clearly annotated.

Both architect reviews (Winston APPROVE, GPT-5.2 PASS) confirm the implementation respects SDK boundaries and uses the intended integration API.

### Refactoring Performed
None — no refactoring needed for this story.

### Compliance Check
- Coding Standards: PASS — `from __future__ import annotations`, type hints on public functions, async/await used correctly, `noqa: S104` on `host="0.0.0.0"` bind
- Project Structure: PASS — `examples/` directory with `__init__.py`, workflows in `examples/workflows/`, smoke test in `tests/integration/`, `__init__.py` present in `tests/integration/`
- Testing Strategy: PASS — Two smoke tests cover health endpoint (AC 4) and blocking endpoint reachability with mocked executor (AC 9). Tests use `httpx.AsyncClient` with `ASGITransport`, bare `async def` consistent with `asyncio_mode=auto`. Mocking targets `WorkflowExecutor.execute` at the correct layer.
- All ACs Met: PASS — All 10 acceptance criteria verified (see gate file for details)

### Improvements Checklist
- [x] Inline comments in example app (AC 8)
- [x] README Quick Start with install + code + run instructions (AC 6)
- [x] README API Reference covers all required types (AC 7)
- [x] Health check endpoint tested (AC 4, 9)
- [x] Blocking endpoint tested with mocked executor (AC 9)
- [ ] SSE streaming endpoint test — only blocking endpoint has a smoke test; no test verifies `/agent/streaming` response headers or SSE content-type (noted by GPT-5.2, tracked as TEST-001)
- [ ] README `WorkflowExecutor` example omits `resolver` parameter — defensible (internal concern with default) but could confuse advanced users (noted by Winston, tracked as DOC-001)

### Security Review
- `host="0.0.0.0"` bind in `__main__` block correctly suppressed with `noqa: S104` — acceptable for a reference app, not shipped in production
- No hardcoded credentials or API keys
- No user input passed unsanitized to system calls
- Example workflows reference `$input.prompt` which is resolved by the SDK's `VariableResolver` — no injection risk at this layer

### Performance Considerations
- Module-level YAML parsing (`_parser.parse_file()` at import time) means workflow files are read once at startup. This is a deliberate fail-fast pattern — if YAML files are missing or malformed, the app fails immediately rather than on first request. Acceptable for a reference application.
- No performance concerns for the example app scope.

### Files Modified During Review
None

### Gate Status
Gate: PASS → docs/qa/gates/5.3-example-app-and-docs.yml

### Recommended Status
Ready for Done
